## Brief introduction
Use this document as prompt context for AI-assisted coding in Cursor. It defines one focused part of the ANSVK system so generated Flutter code stays consistent with Riverpod + go_router, token governance, and reusable component contracts.

Great—this is the foundation of your entire ERP ecosystem. I’ll design this as a production-grade UI system, not just styling.

We’ll define a complete ANSVK Design System you can plug into Cursor IDE and scale across:

Inventory (ANSV/ANSONE)

HR

Service modules



---

🧠 ANSVK UI Philosophy (Important)

Your advantage is:

Efficiency

Data clarity

Professional (not flashy startup UI)


👉 So the style is:

> “Clean Enterprise + Subtle Luxury + Data-first”



Think:

Not playful

Not boring

Confident + structured + scalable



---

🎨 1. COLOR SYSTEM (Premium + Practical)

🔹 Core Palette

class AppColors {
  // Primary (Deep professional blue)
  static const primary = Color(0xFF0F172A);

  // Accent (Modern indigo – action color)
  static const accent = Color(0xFF6366F1);

  // Background
  static const bg = Color(0xFFF8FAFC);

  // Surfaces
  static const surface = Color(0xFFFFFFFF);

  // Borders
  static const border = Color(0xFFE2E8F0);

  // Text
  static const textPrimary = Color(0xFF0F172A);
  static const textSecondary = Color(0xFF64748B);

  // Status
  static const success = Color(0xFF10B981);
  static const warning = Color(0xFFF59E0B);
  static const danger  = Color(0xFFEF4444);
}

💡 Rules

❌ No random colors

✅ Only semantic usage (success, warning, etc.)

✅ Accent = buttons / active states only



---

📏 2. SPACING SYSTEM (Strict = Professional UI)

class Spacing {
  static const xs = 4.0;
  static const sm = 8.0;
  static const md = 16.0;
  static const lg = 24.0;
  static const xl = 32.0;
}

💡 Rules

Page padding → lg

Card padding → md

Gap between sections → xl


👉 Consistency = “luxury feel”


---

🔲 3. BORDER RADIUS + SHADOW

class Radius {
  static const sm = 8.0;
  static const md = 12.0;
  static const lg = 16.0;
}

class Shadows {
  static const card = BoxShadow(
    color: Color(0x0F000000),
    blurRadius: 10,
    offset: Offset(0, 4),
  );
}

💡 Style Direction

Slight rounding → modern

Soft shadow → depth, not heavy



---

🔤 4. TYPOGRAPHY SYSTEM (Data-first readability)

class AppTextStyles {
  static const h1 = TextStyle(fontSize: 28, fontWeight: FontWeight.bold);
  static const h2 = TextStyle(fontSize: 22, fontWeight: FontWeight.w600);
  static const h3 = TextStyle(fontSize: 18, fontWeight: FontWeight.w600);

  static const body = TextStyle(fontSize: 14);
  static const caption = TextStyle(fontSize: 12, color: AppColors.textSecondary);
}

💡 Rules

Avoid too many sizes

Hierarchy must be obvious

Tables → always 14px (readability)



---

🧱 5. CORE COMPONENT SYSTEM (CRITICAL)

You should standardize these:


---

🧩 AppScaffold

class AppScaffold extends StatelessWidget {
  final Widget child;
  const AppScaffold({required this.child});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: AppColors.bg,
      body: Padding(
        padding: const EdgeInsets.all(Spacing.lg),
        child: child,
      ),
    );
  }
}


---

🧩 AppCard (MOST USED)

class AppCard extends StatelessWidget {
  final Widget child;
  const AppCard({required this.child});

  @override
  Widget build(BuildContext context) {
    return Container(
      padding: const EdgeInsets.all(Spacing.md),
      decoration: BoxDecoration(
        color: AppColors.surface,
        borderRadius: BorderRadius.circular(Radius.lg),
        boxShadow: [Shadows.card],
        border: Border.all(color: AppColors.border),
      ),
      child: child,
    );
  }
}


---

🧩 AppButton

class AppButton extends StatelessWidget {
  final String label;
  final VoidCallback onPressed;

  const AppButton({required this.label, required this.onPressed});

  @override
  Widget build(BuildContext context) {
    return ElevatedButton(
      style: ElevatedButton.styleFrom(
        backgroundColor: AppColors.accent,
        padding: const EdgeInsets.symmetric(
          horizontal: Spacing.lg,
          vertical: Spacing.sm,
        ),
        shape: RoundedRectangleBorder(
          borderRadius: BorderRadius.circular(Radius.md),
        ),
      ),
      onPressed: onPressed,
      child: Text(label),
    );
  }
}


---

🧩 AppTable (ERP Core)

👉 You MUST standardize tables early

class AppTable extends StatelessWidget {
  final List<DataColumn> columns;
  final List<DataRow> rows;

  const AppTable({required this.columns, required this.rows});

  @override
  Widget build(BuildContext context) {
    return AppCard(
      child: DataTable(columns: columns, rows: rows),
    );
  }
}


---

🧭 6. LAYOUT SYSTEM (FOR ALL MODULES)

Standard Page Layout

Page
 ├── Header (title + actions)
 ├── Summary Cards (optional)
 ├── Main Content (table/form)


---

Example Structure

Column(
  crossAxisAlignment: CrossAxisAlignment.start,
  children: [
    PageHeader(),
    SizedBox(height: Spacing.lg),
    SummarySection(),
    SizedBox(height: Spacing.lg),
    Expanded(child: AppTable(...)),
  ],
)


---

✨ 7. ANIMATION SYSTEM (Subtle = Premium)

Rules

Duration: 150–250ms

Use only:

Fade

Scale (hover)

Slight elevation



Example

AnimatedContainer(
  duration: Duration(milliseconds: 200),
  transform: isHovered ? Matrix4.identity()..scale(1.02) : Matrix4.identity(),
)


---

🧠 8. CURSOR PROMPT TEMPLATE (USE THIS!)

Paste this into Cursor IDE:

Use ANSVK UI Design System:

- Colors: AppColors
- Spacing: Spacing
- Radius: Radius
- Components: AppCard, AppButton, AppTable

Create a [SCREEN NAME]:

Requirements:
- Clean enterprise UI
- Card-based layout
- Consistent spacing
- Responsive (web)
- Extract reusable widgets

Do NOT hardcode styles outside design system.


---

🚀 9. YOUR COMPETITIVE ADVANTAGE

If you follow this system:

👉 You will have:

Consistent UI across all apps

Faster development (AI reuse)

Easier scaling to ERP


👉 Most competitors in Myanmar:

Inconsistent UI ❌

No system ❌


You win with:

> System + Discipline




---

🔥 Next Step (Important)

I can now design:

✅ Inventory UI (Receive, Issue, Stock)

✅ HR UI (Employee, Leave, Approval)

✅ Dashboard (Executive level)

All using this system.

👉 Tell me: “Design Inventory Module UI” or “Design HR Module UI”

---

## Canonical Flutter stack (accepted) & related docs
* **Riverpod** + **go_router** for new Flutter UI and navigation.
* **Guardrails for AI in Cursor:** `8 Component contracts.md`, `9 Flutter UI conventions.md` (includes **symlink** preference for `docs/ui` → this vault), `10 Page patterns.md`, `11 Figma to implementation.md`.
